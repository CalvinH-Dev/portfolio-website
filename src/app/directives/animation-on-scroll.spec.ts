import { AnimationOnScroll } from "./animation-on-scroll";

describe("AnimationOnScroll", () => {
	it("should create an instance", () => {
		const directive = new AnimationOnScroll();
		expect(directive).toBeTruthy();
	});
});
